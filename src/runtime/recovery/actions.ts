import type { RecoveryActionDescriptor } from './types';

export const RECOVERY_ACTIONS = {
  probeAgain: {
    id: 'recovery.probe_again',
    title: 'Run capability probe again',
    description: 'Run the read-only recovery probe again and compare evidence.',
    class: 'unknown',
    risk: 'readonly',
    confirmation: 'none',
    localOnly: true,
    boundedTo: [],
  },
  refreshRepository: {
    id: 'recovery.refresh_repository',
    title: 'Refresh repository metadata',
    description: 'Refresh registered repository and checkout metadata.',
    class: 'local_recoverable',
    risk: 'low',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['registered_repository'],
  },
  rebuildProjection: {
    id: 'recovery.rebuild_projection',
    title: 'Rebuild runtime projection',
    description: 'Rebuild materialized repo-harness runtime projection from durable state.',
    class: 'stale_runtime_state',
    risk: 'low',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_runtime_storage'],
  },
  reconcileJobs: {
    id: 'recovery.reconcile_jobs',
    title: 'Reconcile jobs and leases',
    description: 'Run a bounded maintenance pass for stale local jobs, leases, and runtime state.',
    class: 'local_jobs_reconciliation_required',
    risk: 'medium',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_runtime_storage'],
  },
  localJobsReconcile: {
    id: 'recovery.local_jobs_reconcile',
    title: 'Reconcile Local Jobs',
    description: 'Terminalize stale active Local Jobs and rebuild the Local Bridge active index without using repository_command_execute.',
    class: 'local_jobs_reconciliation_required',
    risk: 'low',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_runtime_storage', 'local_jobs_metadata'],
  },
  quarantineUnreadableLocalJobs: {
    id: 'recovery.local_jobs_quarantine_unreadable',
    title: 'Quarantine unreadable Local Jobs',
    description: 'Move malformed or missing Local Job metadata into repo-harness quarantine so runtime storage can continue safely.',
    class: 'local_jobs_unreadable',
    risk: 'low',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_runtime_storage', 'local_jobs_quarantine'],
  },
  finalizeRuntimeStorageRelocation: {
    id: 'recovery.runtime_storage_finalize_relocation',
    title: 'Finalize runtime storage relocation',
    description: 'Retry repo-harness runtime storage binding after bounded maintenance removes local metadata blockers.',
    class: 'runtime_storage_not_ready',
    risk: 'low',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_runtime_storage'],
  },
  cleanupPreview: {
    id: 'recovery.cleanup_preview',
    title: 'Preview runtime cleanup',
    description: 'Preview stale repo-harness temp dirs, terminal local jobs, and historical attention cleanup candidates.',
    class: 'local_recoverable',
    risk: 'readonly',
    confirmation: 'none',
    localOnly: true,
    boundedTo: ['repo_harness_runtime_storage'],
  },
  cleanupApply: {
    id: 'recovery.cleanup_apply',
    title: 'Apply runtime cleanup',
    description: 'Apply safe repo-harness runtime cleanup candidates after explicit authorization. Does not kill processes or touch source files.',
    class: 'local_recoverable',
    risk: 'medium',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_runtime_storage', 'repo_harness_temp_dirs'],
  },
  restartLocalBridge: {
    id: 'recovery.restart_local_bridge',
    title: 'Restart local bridge',
    description: 'Restart only the repo-harness local bridge process. The current HTTP request may disconnect.',
    class: 'local_recoverable',
    risk: 'medium',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_local_bridge'],
  },
  restartController: {
    id: 'recovery.restart_controller',
    title: 'Restart controller daemon',
    description: 'Restart the repo-harness controller daemon and verify readiness after restart.',
    class: 'local_recoverable',
    risk: 'high',
    confirmation: 'strong_confirmation',
    localOnly: true,
    boundedTo: ['repo_harness_controller_daemon'],
  },
  createSelfFixTask: {
    id: 'recovery.create_self_fix_task',
    title: 'Create self-fix task',
    description: 'Create a bounded source-fix task in an isolated worktree when evidence points to a repo-harness defect.',
    class: 'source_defect_suspected',
    risk: 'high',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_source_repository'],
  },
  createPatchHandoff: {
    id: 'recovery.create_patch_handoff',
    title: 'Create patch handoff',
    description: 'Create a safe patch handoff artifact when client-side tool calls are blocked.',
    class: 'platform_blocked',
    risk: 'medium',
    confirmation: 'authorization',
    localOnly: true,
    boundedTo: ['repo_harness_artifact_storage'],
  },
  workspaceAuthLoginPrepare: {
    id: 'recovery.workspace_auth_login_prepare',
    title: 'Prepare Workspace auth login',
    description: 'Prepare a non-secret Gmail/Google Workspace login handoff when plugin auth is missing.',
    class: 'auth_required',
    risk: 'readonly',
    confirmation: 'none',
    localOnly: true,
    boundedTo: ['google_workspace_auth_handoff'],
  },
  browserDomainAccessPreview: {
    id: 'recovery.browser_domain_access_preview',
    title: 'Preview browser domain grant',
    description: 'Preview a domain-key browser grant instead of retrying arbitrary URLs.',
    class: 'browser_domain_grant_required',
    risk: 'readonly',
    confirmation: 'none',
    localOnly: true,
    boundedTo: ['browser_allowed_domains'],
  },
  externalFilesystemGrantPreview: {
    id: 'recovery.external_filesystem_grant_preview',
    title: 'Preview external filesystem grant',
    description: 'Preview a narrow external filesystem target grant instead of retrying absolute paths.',
    class: 'external_filesystem_grant_required',
    risk: 'readonly',
    confirmation: 'none',
    localOnly: true,
    boundedTo: ['external_filesystem_grants'],
  },
} satisfies Record<string, RecoveryActionDescriptor>;

export function recoveryActionById(actionId: string): RecoveryActionDescriptor | undefined {
  return Object.values(RECOVERY_ACTIONS).find((action) => action.id === actionId);
}
