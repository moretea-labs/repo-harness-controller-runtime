function rawUnicodeTemplate(strings: TemplateStringsArray, ...values: unknown[]): string {
  return strings.raw.map((chunk, index) => {
    const decoded = chunk.replace(/\\u([0-9a-fA-F]{4})/g, (_match, hex: string) =>
      String.fromCharCode(Number.parseInt(hex, 16)),
    );
    return decoded + (index < values.length ? String(values[index]) : "");
  }).join("");
}

export function localBridgeDashboardHtml(): string {
  return rawUnicodeTemplate`<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>repo-harness · 执行助手控制台</title>
<style>
:root{color-scheme:dark;--bg:#050708;--side:#090d10;--top:#0d1115;--panel:#10161a;--panel2:#151b20;--glass:rgba(18,27,31,.78);--line:rgba(255,255,255,.09);--line2:rgba(255,255,255,.16);--text:#f5f7f8;--muted:#a6adb5;--faint:#707982;--green:#55e28a;--green2:#2bbd69;--amber:#f59e0b;--red:#fb7185;--blue:#60a5fa;--purple:#a78bfa;--shadow:0 28px 80px rgba(0,0,0,.42);--radius:20px}
*{box-sizing:border-box}html,body{height:100%;margin:0}body{background:radial-gradient(circle at 72% 3%,rgba(34,197,94,.16),transparent 34%),linear-gradient(135deg,#030506,#081116 54%,#050608);color:var(--text);font:14px/1.55 Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;overflow:hidden}.mono{font-family:"SFMono-Regular",Consolas,"Liberation Mono",Menlo,monospace}.app{height:100vh;display:grid;grid-template-rows:74px minmax(0,1fr);border:1px solid var(--line);border-radius:10px;overflow:hidden;background:rgba(5,7,8,.7)}
.top{display:flex;align-items:center;justify-content:space-between;background:rgba(13,17,21,.82);backdrop-filter:blur(20px);border-bottom:1px solid var(--line);padding:0 22px;gap:18px}.brand{height:100%;min-width:330px;display:flex;align-items:center;gap:14px;border-right:1px solid var(--line);margin-left:-22px;padding-left:22px}.traffic{display:flex;gap:8px}.traffic i{width:13px;height:13px;border-radius:50%;display:block}.traffic i:nth-child(1){background:#ff5f57}.traffic i:nth-child(2){background:#ffbd2e}.traffic i:nth-child(3){background:#28c840}.logo{width:30px;height:30px;border-radius:10px;display:grid;place-items:center;background:rgba(85,226,138,.12);color:var(--green);font-weight:900;box-shadow:0 0 0 1px rgba(85,226,138,.22),0 0 28px rgba(85,226,138,.14)}.brand strong{font-size:18px;letter-spacing:-.02em}.topMiddle{flex:1;display:flex;align-items:center;gap:18px;min-width:0}.repoChip,.statusChip,.pluginChip,.attentionChip{display:inline-flex;align-items:center;gap:8px;border:1px solid var(--line);background:rgba(255,255,255,.045);border-radius:999px;padding:8px 12px;white-space:nowrap}.statusChip{background:rgba(85,226,138,.12);border-color:rgba(85,226,138,.24);color:#b8ffd0;font-weight:800}.dot{width:8px;height:8px;border-radius:50%;background:var(--green);box-shadow:0 0 0 6px rgba(85,226,138,.08)}.dot.amber{background:var(--amber);box-shadow:0 0 0 6px rgba(245,158,11,.08)}.dot.red{background:var(--red);box-shadow:0 0 0 6px rgba(251,113,133,.08)}.topRight{display:flex;align-items:center;gap:12px}.iconRail{display:flex;gap:8px;border:1px solid var(--line);background:rgba(255,255,255,.04);border-radius:999px;padding:6px}.circleBtn{width:32px;height:32px;border-radius:999px;border:1px solid var(--line2);background:#10161a;color:var(--text);display:grid;place-items:center}.attentionChip{color:#ffcd7a}.attentionChip b{display:inline-grid;place-items:center;min-width:24px;height:24px;border-radius:999px;background:rgba(245,158,11,.18);color:#ffbd59}.avatar{width:38px;height:38px;border-radius:999px;display:grid;place-items:center;background:#171c21;border:1px solid var(--line2);font-weight:900;position:relative}.avatar:after{content:"";position:absolute;right:2px;bottom:2px;width:9px;height:9px;border-radius:50%;background:var(--green);border:2px solid #171c21}
.body{min-height:0;display:grid;grid-template-columns:282px minmax(0,1fr)}.side{min-height:0;padding:22px 18px;background:linear-gradient(180deg,rgba(7,11,14,.98),rgba(6,8,10,.96));border-right:1px solid var(--line);display:flex;flex-direction:column;justify-content:space-between}.nav{display:grid;gap:8px}.nav button{display:flex;align-items:center;gap:14px;width:100%;border:0;background:transparent;color:#c7cdd3;text-align:left;padding:14px 18px;border-radius:14px;font-size:16px;cursor:pointer}.nav button:hover{background:rgba(255,255,255,.045)}.nav button.active{background:linear-gradient(135deg,rgba(85,226,138,.18),rgba(85,226,138,.07));color:#b9ffd1;box-shadow:inset 0 0 0 1px rgba(85,226,138,.18)}.nav .badge{margin-left:auto}.navSep{height:1px;background:var(--line);margin:18px 0}.help{width:38px;height:38px;border-radius:999px;border:1px solid var(--line);background:rgba(255,255,255,.04);color:var(--muted)}.main{min-width:0;min-height:0;overflow:auto;padding:22px 24px 44px;scrollbar-color:rgba(255,255,255,.18) transparent}.view{display:none}.view.active{display:block}.hero{position:relative;overflow:hidden;border:1px solid rgba(85,226,138,.18);border-radius:22px;padding:30px 34px;background:radial-gradient(circle at 80% 38%,rgba(85,226,138,.2),transparent 22%),linear-gradient(135deg,rgba(24,54,37,.78),rgba(10,17,18,.92) 60%,rgba(9,12,14,.96));box-shadow:var(--shadow);min-height:330px}.hero:before{content:"";position:absolute;inset:0;background:linear-gradient(110deg,transparent 0 62%,rgba(85,226,138,.08) 62%,transparent 92%);pointer-events:none}.heroGrid{position:relative;display:grid;grid-template-columns:minmax(0,1fr) 340px;gap:30px;align-items:center}.readyIcon{width:64px;height:64px;border-radius:999px;display:grid;place-items:center;border:1px solid rgba(85,226,138,.5);background:rgba(85,226,138,.12);box-shadow:0 0 36px rgba(85,226,138,.35);color:#caffdc;font-size:34px}.heroTitle{display:flex;align-items:center;gap:18px}.hero h1{font-size:54px;line-height:1;margin:0 0 8px;letter-spacing:-.06em}.hero p{margin:0;color:#d5dbdf;font-size:17px}.chips{display:flex;gap:10px;flex-wrap:wrap;margin:24px 0 20px}.chip{display:inline-flex;align-items:center;gap:8px;border:1px solid rgba(255,255,255,.14);background:rgba(0,0,0,.2);border-radius:999px;padding:8px 12px;color:#c9d2d8}.next{color:var(--green);font-weight:850;margin-top:12px}.nextLine{font-size:18px;color:#f0f4f2;margin:6px 0 18px}.ctaRow{display:grid;grid-template-columns:1.05fr 1fr 1fr 1fr;gap:14px}.cta{border:1px solid var(--line2);background:rgba(255,255,255,.07);color:var(--text);border-radius:16px;min-height:76px;padding:12px 16px;display:flex;align-items:center;gap:14px;cursor:pointer;text-align:left}.cta:hover{border-color:rgba(85,226,138,.4);background:rgba(255,255,255,.1)}.cta.primary{background:linear-gradient(135deg,#57df87,#24a95d);border-color:rgba(85,226,138,.55);color:white;box-shadow:0 18px 44px rgba(43,189,105,.28)}.ctaIcon{font-size:28px}.cta strong{display:block;font-size:17px}.cta span{display:block;color:rgba(255,255,255,.68);font-size:12px}.radar{position:relative;width:270px;height:270px;margin:auto;border-radius:999px;background:radial-gradient(circle,rgba(85,226,138,.22),rgba(85,226,138,.08) 33%,transparent 58%)}.radar:before,.radar:after{content:"";position:absolute;inset:20px;border-radius:999px;border:1px solid rgba(85,226,138,.36);box-shadow:0 0 38px rgba(85,226,138,.18)}.radar:after{inset:58px;border-width:2px}.radarCheck{position:absolute;inset:88px;border-radius:999px;display:grid;place-items:center;border:2px solid rgba(85,226,138,.72);box-shadow:0 0 44px rgba(85,226,138,.38);font-size:68px;color:#dcffe7}.grid{display:grid;gap:14px}.cards4{grid-template-columns:repeat(4,minmax(0,1fr));margin-top:14px}.panel{border:1px solid var(--line);border-radius:var(--radius);background:linear-gradient(145deg,rgba(18,24,29,.92),rgba(11,14,17,.92));box-shadow:var(--shadow)}.card{padding:20px;min-height:180px}.cardHead{display:flex;align-items:center;gap:14px;margin-bottom:8px}.cardIcon{width:42px;height:42px;border-radius:15px;display:grid;place-items:center;background:rgba(85,226,138,.14);color:var(--green);font-size:22px}.cardIcon.blue{background:rgba(96,165,250,.14);color:var(--blue)}.cardIcon.amber{background:rgba(245,158,11,.14);color:var(--amber)}.cardIcon.purple{background:rgba(167,139,250,.14);color:var(--purple)}.card h2{font-size:16px;margin:0}.card strong.big{font-size:25px;display:block;margin:6px 0 4px}.muted{color:var(--muted)}.faint{color:var(--faint)}button{font:inherit}.smallBtn,.primaryBtn,.dangerBtn,.ghostBtn{border:1px solid var(--line2);border-radius:12px;background:rgba(255,255,255,.07);color:var(--text);padding:9px 14px;cursor:pointer}.primaryBtn{background:linear-gradient(135deg,#57df87,#24a95d);border-color:rgba(85,226,138,.5);font-weight:850}.dangerBtn{background:rgba(251,113,133,.12);border-color:rgba(251,113,133,.3);color:#ffb2c0}.ghostBtn{background:transparent}.card .smallBtn{margin-top:14px;float:right}.section{margin-top:14px;padding:20px}.sectionHead{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.sectionHead h2{font-size:18px;margin:0}.list{display:grid}.row{display:grid;grid-template-columns:32px minmax(0,1fr) 120px 110px 28px;gap:12px;align-items:center;padding:12px 4px;border-top:1px solid var(--line)}.row:first-child{border-top:0}.rowTitle{font-weight:800}.pill{display:inline-flex;align-items:center;justify-content:center;border-radius:999px;padding:5px 10px;font-size:12px;font-weight:850}.pill.green{background:rgba(85,226,138,.15);color:#84f3aa}.pill.blue{background:rgba(96,165,250,.14);color:#92c5ff}.pill.amber{background:rgba(245,158,11,.16);color:#ffc36a}.pill.red{background:rgba(251,113,133,.15);color:#ffb0bd}.pill.gray{background:rgba(255,255,255,.08);color:#c4cbd1}.inlineForm{display:grid;grid-template-columns:minmax(260px,1fr) 180px auto;gap:10px;margin:14px 0}.input{border:1px solid var(--line2);background:rgba(0,0,0,.24);border-radius:12px;color:var(--text);padding:12px 13px;outline:none}.pageHead{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:16px}.pageHead h1{font-size:30px;margin:0}.pageHead p{margin:4px 0 0;color:var(--muted)}.twoCol{display:grid;grid-template-columns:1fr 1fr;gap:14px}.repoCard,.attentionCard,.pluginCard{padding:18px}.repoCard{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center}.attentionCard{display:grid;grid-template-columns:48px minmax(0,1fr) auto;gap:14px;align-items:center}.pluginCard{display:grid;grid-template-columns:48px minmax(0,1fr) auto;gap:14px;align-items:center}.empty{border:1px dashed var(--line2);border-radius:16px;padding:24px;text-align:center;color:var(--muted)}.hidden{display:none!important}.toast{position:fixed;right:22px;bottom:22px;background:#111820;border:1px solid var(--line2);border-radius:14px;padding:12px 16px;box-shadow:var(--shadow);opacity:0;transform:translateY(8px);transition:.2s;z-index:20}.toast.show{opacity:1;transform:none}@media(max-width:1200px){.heroGrid{grid-template-columns:1fr}.radar{display:none}.ctaRow,.cards4,.twoCol{grid-template-columns:1fr 1fr}.row{grid-template-columns:32px minmax(0,1fr) 90px 90px 24px}.brand{min-width:260px}}@media(max-width:860px){body{overflow:auto}.app{height:auto;min-height:100vh}.body{display:block}.side{display:none}.main{overflow:visible}.ctaRow,.cards4,.twoCol{grid-template-columns:1fr}.top{flex-wrap:wrap;height:auto;padding:12px}.brand{height:auto;border-right:0}.topRight{display:none}.hero h1{font-size:42px}.inlineForm{grid-template-columns:1fr}}
</style>
</head>
<body>
<div class="app">
  <header class="top">
    <div class="brand"><span class="traffic"><i></i><i></i><i></i></span><span class="logo">∞</span><strong>repo-harness</strong></div>
    <div class="topMiddle"><span class="repoChip">▣ 当前仓库：<b id="topRepo">—</b></span><span class="statusChip"><span class="dot" id="topReadyDot"></span><span id="topReady">检查中</span></span><span class="pluginChip">插件 <b id="topPlugins">—</b> 可用</span></div>
    <div class="topRight"><div class="iconRail"><span class="circleBtn">GH</span><span class="circleBtn">🌐</span><span class="circleBtn">M</span><span class="circleBtn">＋</span></div><button class="attentionChip" onclick="switchView('attention')">🔔 待处理 <b id="topAttention">0</b></button><span class="avatar">G</span></div>
  </header>
  <div class="body">
    <aside class="side"><div><nav class="nav">
      <button class="active" data-view="home">⌂ 首页</button>
      <button data-view="work">▷ 执行任务</button>
      <button data-view="attention">☑ 待处理 <span class="badge pill amber" id="sideAttention">0</span></button>
      <button data-view="repositories">▤ 仓库</button>
      <button data-view="capabilities">✣ 能力</button>
      <button data-view="history">◷ 历史</button>
      <div class="navSep"></div>
      <button data-view="advanced">⌁ 高级诊断</button>
    </nav></div><button class="help">?</button></aside>
    <main class="main">
      <section class="view active" id="view-home"></section>
      <section class="view" id="view-work"></section>
      <section class="view" id="view-attention"></section>
      <section class="view" id="view-repositories"></section>
      <section class="view" id="view-capabilities"></section>
      <section class="view" id="view-history"></section>
      <section class="view" id="view-advanced"></section>
    </main>
  </div>
</div>
<div class="toast" id="toast"></div>
<div class="hidden" id="legacyMarkers">概览 工作项 活动 设置 Direct Edit Agent 输出 查看原始日志 /events parseAgentOutput Ready for Review Needs Attention Assistant Plugins Mobile Intents</div>
<script>
var model=null;var raw=null;var activeView='home';var selectedRepoId=null;var stream=null;
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c]})}
function arr(v){return Array.isArray(v)?v:[]}
function obj(v){return v&&typeof v==='object'&&!Array.isArray(v)?v:{}}
function fmt(v){if(!v)return '—';try{return new Date(v).toLocaleString()}catch(_e){return String(v)}}
function short(v,n){v=String(v==null?'':v);return v.length>(n||72)?v.slice(0,(n||72)-1)+'…':v}
function scopedPath(path){if(!selectedRepoId)return path;return path+(path.indexOf('?')>=0?'&':'?')+'repoId='+encodeURIComponent(selectedRepoId)}
function connectStream(){if(stream){stream.close();stream=null}if(!window.EventSource)return;stream=new EventSource(scopedPath('/api/stream'));stream.addEventListener('refresh',function(){refresh()});stream.addEventListener('error',function(){if(stream){stream.close();stream=null}setTimeout(connectStream,3500)})}
function api(path,opts){opts=opts||{};var headers=Object.assign({'content-type':'application/json'},opts.headers||{});return fetch(scopedPath(path),Object.assign({},opts,{credentials:'same-origin',headers:headers})).then(function(r){return r.json().catch(function(){return{}}).then(function(body){if(!r.ok)throw new Error(body.error||body.message||String(r.status));return body})})}
function toast(msg){var t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');setTimeout(function(){t.classList.remove('show')},3800)}
function switchView(name){activeView=name;document.querySelectorAll('.nav button').forEach(function(b){b.classList.toggle('active',b.dataset.view===name)});document.querySelectorAll('.view').forEach(function(v){v.classList.toggle('active',v.id==='view-'+name)});renderAll()}
document.querySelectorAll('.nav button[data-view]').forEach(function(b){b.addEventListener('click',function(){switchView(b.dataset.view)})});
function statusPill(status){var cls=status==='completed'||status==='ready'||status==='connected'||status==='healthy'||status==='enabled'?'green':status==='running'?'blue':status==='attention'||status==='needs_setup'||status==='needs_action'||status==='authorization_required'?'amber':status==='failed'||status==='blocked'||status==='error'?'red':'gray';var label=({completed:'已完成',running:'执行中',attention:'需要处理',ready:'就绪',needs_setup:'需配置',connected:'已连接',healthy:'可用',failed:'失败',blocked:'阻塞',enabled:'已启用',authorization_required:'待授权'})[status]||status||'未知';return '<span class="pill '+cls+'">'+esc(label)+'</span>'}
function iconForStatus(status){if(status==='completed'||status==='ready'||status==='connected'||status==='healthy')return '✓';if(status==='running')return '◌';if(status==='failed'||status==='blocked')return '!';return '!' }
function viewAction(action){if(!action)return;if(action.id==='start_task'){switchView('work');setTimeout(function(){var x=document.getElementById('taskInput');if(x)x.focus()},80)}else if(action.id==='manage_repositories'){switchView('repositories')}else if(action.id==='configure_plugins'){switchView('capabilities')}else if(action.id==='handle_attention'){switchView('attention')}else if(action.id==='view_advanced'){switchView('advanced')}else if(action.id==='view_history'){switchView('history')}else{toast('该动作已进入详情页：'+(action.label||action.id))}}
function renderChrome(){var r=obj(model&&model.readiness);var repo=obj(model&&model.currentRepository);var plug=obj(model&&model.pluginSummary);var attention=arr(model&&model.attentionItems).length;document.getElementById('topRepo').textContent=repo.name||'未选择';document.getElementById('topReady').textContent=r.label||'未知';document.getElementById('topReadyDot').className='dot '+(r.state==='blocked'?'red':r.state==='needs_setup'?'amber':'');document.getElementById('topPlugins').textContent=(plug.ready||0)+'/'+(plug.total||0);document.getElementById('topAttention').textContent=attention;document.getElementById('sideAttention').textContent=attention}
function renderHome(){var r=obj(model.readiness);var repo=obj(model.currentRepository);var plug=obj(model.pluginSummary);var att=arr(model.attentionItems);var rec=obj(model.recommendation);var recent=arr(model.recentTasks).slice(0,3);var chips=arr(r.chips).map(function(c){return '<span class="chip"><span class="dot '+(c.tone==='amber'?'amber':c.tone==='red'?'red':'')+'"></span>'+esc(c.label)+'</span>'}).join('');var hero='<div class="hero"><div class="heroGrid"><div><div class="heroTitle"><div class="readyIcon">'+(r.state==='blocked'?'!':'✓')+'</div><div><h1>'+esc(r.title||'系统状态')+'</h1><p>'+esc(r.description||'正在读取 controller 状态')+'</p></div></div><div class="chips">'+chips+'</div><div class="next">推荐下一步</div><div class="nextLine">☆ '+esc(rec.title||'创建一个新任务，让助手为你完成工作')+'</div><div class="ctaRow">'+renderCta({id:'start_task',label:'执行一个任务',hint:'让助手立即开始',icon:'▶',primary:true})+renderCta({id:'manage_repositories',label:'管理仓库',hint:'查看与管理当前仓库',icon:'▱'})+renderCta({id:'configure_plugins',label:'配置插件',hint:'连接更多能力',icon:'✣'})+renderCta({id:'handle_attention',label:'处理待办',hint:'查看并处理待办事项',icon:'☑',badge:att.length})+'</div></div><div class="radar"><div class="radarCheck">✓</div></div></div></div>';var cards='<div class="grid cards4">'+summaryCard('当前仓库','▣','',repo.name||'未选择',repo.path||'请先添加仓库',repo.statusLabel||'—','管理仓库','repositories')+summaryCard('插件能力','✣','blue',(plug.ready||0)+'/'+(plug.total||0)+' 可用',arr(plug.lines).join('\n')||'暂无插件状态','', '配置插件','capabilities')+summaryCard('待处理事项','☑','amber',att.length+' 项待处理',attentionSummary(att),'','去处理','attention')+summaryCard('今日建议','♢','purple',rec.title||'先处理高优先级事项',rec.description||'当前系统可用，你可以直接输入任务。','','查看详情','attention')+'</div>';var history='<div class="panel section"><div class="sectionHead"><h2>最近任务</h2><button class="ghostBtn" onclick="switchView(\'history\')">查看全部历史任务 ›</button></div><div class="list">'+(recent.map(renderTaskRow).join('')||'<div class="empty">暂无最近任务</div>')+'</div></div>';document.getElementById('view-home').innerHTML=hero+cards+history}
function renderCta(a){return '<button class="cta '+(a.primary?'primary':'')+'" onclick="viewAction({id:\''+esc(a.id)+'\',label:\''+esc(a.label)+'\'})"><span class="ctaIcon">'+esc(a.icon)+'</span><span><strong>'+esc(a.label)+(a.badge?'<span class="pill amber" style="margin-left:8px">'+esc(a.badge)+'</span>':'')+'</strong><span>'+esc(a.hint)+'</span></span></button>'}
function summaryCard(title,icon,tone,big,body,status,button,view){return '<div class="panel card"><div class="cardHead"><div class="cardIcon '+esc(tone)+'">'+esc(icon)+'</div><h2>'+esc(title)+'</h2></div><strong class="big">'+esc(big)+'</strong><p class="muted" style="white-space:pre-line">'+esc(body)+'</p>'+(status?'<p>'+statusPill(status)+'</p>':'')+'<button class="smallBtn" onclick="switchView(\''+esc(view)+'\')">'+esc(button)+'</button></div>'}
function attentionSummary(items){if(!items.length)return '当前没有需要你处理的事项';var counts={};items.forEach(function(x){counts[x.categoryLabel]=(counts[x.categoryLabel]||0)+1});return Object.keys(counts).slice(0,3).map(function(k){return '• '+counts[k]+' 个'+k}).join('\n')}
function renderTaskRow(t){return '<div class="row"><div class="pill '+(t.status==='completed'?'green':t.status==='running'?'blue':t.status==='failed'?'red':'amber')+'">'+iconForStatus(t.status)+'</div><div><div class="rowTitle">'+esc(t.title)+'</div><div class="muted">'+esc(t.summary||'')+'</div></div><div class="faint">'+esc(t.actor||'G')+'</div><div>'+statusPill(t.status)+'</div><button class="ghostBtn" onclick="switchView(\'history\')">›</button></div>'}
function renderWork(){document.getElementById('view-work').innerHTML='<div class="pageHead"><div><h1>执行任务</h1><p>直接输入自然语言目标，controller 会检查仓库和能力后启动执行。</p></div></div><div class="panel section"><div class="inlineForm"><input class="input" id="taskInput" placeholder="例如：检查当前项目的导出功能并修复明显问题" /><select class="input" id="taskMode"><option value="execute">立即执行</option><option value="plan_then_execute">先规划再执行</option></select><button class="primaryBtn" onclick="submitTask()">执行任务</button></div><p class="muted">系统会优先展示可决策的信息，原始日志放在历史或高级诊断中。</p></div>'+renderRecentPanel('执行中的任务',arr(model.recentTasks).filter(function(t){return t.status==='running'}))}
function submitTask(){var input=document.getElementById('taskInput');var utterance=input&&input.value?input.value.trim():'';if(!utterance){toast('请输入任务内容');return}api('/api/assistant/intent',{method:'POST',body:JSON.stringify({utterance:utterance,source:'local-ui',mode:document.getElementById('taskMode').value})}).then(function(r){toast('任务已提交：'+(r.requestId||r.jobId||'accepted'));input.value='';refresh().then(connectStream);switchView('history')}).catch(function(e){toast(e.message)})}
function renderAttention(){var items=arr(model.attentionItems);var grouped={};items.forEach(function(item){var key=item.categoryLabel||'其他';(grouped[key]=grouped[key]||[]).push(item)});var html='<div class="pageHead"><div><h1>待处理</h1><p>这里不展示 controller 内部状态，只展示你需要做的决策。</p></div><button class="ghostBtn" onclick="refresh()">刷新</button></div>';var keys=Object.keys(grouped);if(!keys.length)html+='<div class="empty">没有待处理事项。你可以直接执行新任务。</div>';keys.forEach(function(k){html+='<div class="panel section"><div class="sectionHead"><h2>'+esc(k)+'</h2><span class="pill amber">'+grouped[k].length+'</span></div>'+grouped[k].map(renderAttentionCard).join('')+'</div>'});document.getElementById('view-attention').innerHTML=html}
function renderAttentionCard(item){var primary=obj(item.primaryAction);return '<div class="attentionCard"><div class="cardIcon amber">'+esc(item.icon||'!')+'</div><div><div class="rowTitle">'+esc(item.title)+'</div><div class="muted">'+esc(item.reason)+'</div><div class="faint">'+esc(item.scope||'当前仓库')+'</div></div><div><button class="primaryBtn" onclick="viewAction({id:\''+esc(primary.id||'view_advanced')+'\',label:\''+esc(primary.label||'查看')+'\'})">'+esc(primary.label||'查看')+'</button></div></div>'}
function renderRepositories(){var repos=arr(model.repositories);var html='<div class="pageHead"><div><h1>仓库</h1><p>选择或注册本地项目，controller 只在当前仓库上下文中执行任务。</p></div></div><div class="panel section"><h2>添加本地仓库</h2><div class="inlineForm"><input class="input" id="repoPath" placeholder="/Users/greyson/DevProjects/YourRepo" /><input class="input" id="repoName" placeholder="显示名称（可选）" /><button class="primaryBtn" onclick="registerRepo()">注册仓库</button></div></div><div class="grid twoCol" style="margin-top:14px">'+(repos.map(renderRepoCard).join('')||'<div class="empty">暂无已注册仓库</div>')+'</div>';document.getElementById('view-repositories').innerHTML=html}
function renderRepoCard(repo){return '<div class="panel repoCard"><div><div class="rowTitle">'+esc(repo.name)+'</div><div class="muted mono">'+esc(repo.path)+'</div><div style="margin-top:8px">'+statusPill(repo.status)+'</div></div><button class="smallBtn" onclick="selectRepo(\''+esc(repo.id)+'\')">'+(repo.current?'当前仓库':'切换到此仓库')+'</button></div>'}
function selectRepo(id){selectedRepoId=id||null;connectStream();refresh().then(function(){toast('已切换仓库')}).catch(function(e){toast(e.message)})}
function registerRepo(){var path=document.getElementById('repoPath').value.trim();var name=document.getElementById('repoName').value.trim();if(!path){toast('请输入本地仓库路径');return}api('/api/repositories/register',{method:'POST',body:JSON.stringify({path:path,displayName:name||undefined})}).then(function(){toast('仓库已注册');refresh();switchView('repositories')}).catch(function(e){toast(e.message)})}
function renderCapabilities(){var list=arr(model.plugins);var html='<div class="pageHead"><div><h1>能力</h1><p>插件以“是否连接、能否测试、下一步怎么修复”表达，不默认暴露 manifest。</p></div></div><div class="grid twoCol">'+(list.map(renderPluginCard).join('')||'<div class="empty">暂无插件能力</div>')+'</div>';document.getElementById('view-capabilities').innerHTML=html}
function renderPluginCard(p){return '<div class="panel pluginCard"><div class="cardIcon '+(p.status==='ready'?'':'amber')+'">✣</div><div><div class="rowTitle">'+esc(p.name)+'</div><div class="muted">'+esc(p.description||p.nextStep||'')+'</div><div class="faint">'+esc(p.nextStep||'')+'</div></div><div><div>'+statusPill(p.status)+'</div><button class="smallBtn" onclick="switchView(\'advanced\')">高级详情</button></div></div>'}
function renderHistory(){document.getElementById('view-history').innerHTML='<div class="pageHead"><div><h1>历史</h1><p>查看最近执行、结果和需要追溯的凭证。</p></div></div>'+renderRecentPanel('最近任务',arr(model.recentTasks))}
function renderRecentPanel(title,items){return '<div class="panel section"><div class="sectionHead"><h2>'+esc(title)+'</h2><span class="pill gray">'+items.length+'</span></div><div class="list">'+(items.map(renderTaskRow).join('')||'<div class="empty">暂无记录</div>')+'</div></div>'}
function renderAdvanced(){document.getElementById('view-advanced').innerHTML='<div class="pageHead"><div><h1>高级诊断</h1><p>原始 controller 状态保留在这里，仅用于排错。</p></div><button class="ghostBtn" onclick="refreshRaw()">读取原始快照</button></div><div class="panel section"><pre class="mono" style="white-space:pre-wrap;max-height:58vh;overflow:auto;color:#d7e3ec">'+esc(JSON.stringify(raw||model,null,2))+'</pre></div>'}
function renderAll(){if(!model)return;renderChrome();renderHome();renderWork();renderAttention();renderRepositories();renderCapabilities();renderHistory();renderAdvanced()}
function refresh(){return api('/api/user-snapshot').then(function(s){model=s;if(!selectedRepoId&&s.currentRepository&&s.currentRepository.id)selectedRepoId=s.currentRepository.id;return api('/api/snapshot')}).then(function(s){raw=s;renderAll()}).catch(function(e){toast(e.message)})}
function refreshRaw(){api('/api/snapshot').then(function(s){raw=s;renderAdvanced();toast('原始快照已刷新')}).catch(function(e){toast(e.message)})}
refresh().then(connectStream);
</script>
</body>
</html>`;
}
