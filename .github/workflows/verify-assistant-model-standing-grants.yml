name: Verify Assistant Model Standing Grants

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  verify-and-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/assistant-model-standing-grants
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - run: bun install --frozen-lockfile

      - name: Apply final hardening
        shell: bash
        run: |
          set +e
          python3 scripts/harden-assistant-model-standing-grants.py 2>&1 | tee assistant-model-patch.log
          echo "${PIPESTATUS[0]}" > assistant-model-patch.status
          exit 0

      - name: Type check
        shell: bash
        run: |
          set +e
          bun x tsc --noEmit 2>&1 | tee assistant-model-tsc.log
          echo "${PIPESTATUS[0]}" > assistant-model-tsc.status
          exit 0

      - name: Model and Standing Grant tests
        shell: bash
        run: |
          set +e
          timeout 120s bun test tests/runtime/assistant-model-standing-grants.test.ts 2>&1 | tee assistant-model-tests.log
          echo "${PIPESTATUS[0]}" > assistant-model-tests.status
          exit 0

      - name: Gmail Routine compatibility tests
        shell: bash
        run: |
          set +e
          timeout 120s bun test tests/runtime/gmail-assistant-routines.test.ts 2>&1 | tee gmail-routine-tests.log
          echo "${PIPESTATUS[0]}" > gmail-routine-tests.status
          exit 0

      - name: OAuth History compatibility tests
        shell: bash
        run: |
          set +e
          timeout 120s bun test tests/runtime/google-oauth-history-proposals.test.ts 2>&1 | tee oauth-history-tests.log
          echo "${PIPESTATUS[0]}" > oauth-history-tests.status
          exit 0

      - name: Readiness compatibility tests
        shell: bash
        run: |
          set +e
          timeout 120s bun test tests/runtime/capability-recovery.test.ts 2>&1 | tee readiness-tests.log
          echo "${PIPESTATUS[0]}" > readiness-tests.status
          exit 0

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: assistant-model-standing-grant-diagnostics
          path: |
            assistant-model-patch.log
            assistant-model-patch.status
            assistant-model-tsc.log
            assistant-model-tsc.status
            assistant-model-tests.log
            assistant-model-tests.status
            gmail-routine-tests.log
            gmail-routine-tests.status
            oauth-history-tests.log
            oauth-history-tests.status
            readiness-tests.log
            readiness-tests.status

      - name: Enforce verification
        run: |
          test "$(cat assistant-model-patch.status)" = "0"
          test "$(cat assistant-model-tsc.status)" = "0"
          test "$(cat assistant-model-tests.status)" = "0"
          test "$(cat gmail-routine-tests.status)" = "0"
          test "$(cat oauth-history-tests.status)" = "0"
          test "$(cat readiness-tests.status)" = "0"

      - name: Commit verified hardening
        run: |
          git config user.name "repo-harness automation"
          git config user.email "actions@users.noreply.github.com"
          git add src tests docs
          git diff --cached --quiet && exit 0
          git commit -m "fix(assistant): harden model credentials and grant scope"
          git push origin HEAD:feat/assistant-model-standing-grants
