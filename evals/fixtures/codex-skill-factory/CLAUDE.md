# CLAUDE

Project uses tasks-first workflow with Skill Factory hooks active.

- `tasks/todo.md` is the active execution checklist.
- `tasks/lessons.md` captures correction-derived rules.
- `plans/` is the plan catalog; `.ai/harness/active-plan` selects the active plan.
- Skill Factory hooks collect signal and propose reusable skills.
