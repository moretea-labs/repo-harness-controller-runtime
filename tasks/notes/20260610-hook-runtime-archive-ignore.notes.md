# Hook Runtime Archive Ignore Closeout

## Context

Merging `origin/claude/hook-loop-review-2iejon` surfaced a dirty worktree caused by `.ai/harness/archive/events-202606.jsonl`.

The file is generated by `workflow_rotate_events_file()` when hook event traces rotate. It is local runtime evidence, not a tracked deliverable, and it blocked `scripts/check-task-sync.sh` as an untracked substantive change.

## Decision

Treat `.ai/harness/archive/` as ignored runtime state alongside `.ai/harness/events.jsonl`, `.ai/harness/checks/latest.json`, `.ai/harness/runs/`, and other hook-local state.

Updated the root ignore file, the project-init ignore template, the workflow contract manifests, installed-copy sync exclusions, and the contract test that protects runtime harness artifacts from becoming tracked deliverables.

## Verification

- `bun test` passed before this follow-up fix: 600 pass, 0 fail.
- Re-run required checks after this note and the ignore fix before pushing.
